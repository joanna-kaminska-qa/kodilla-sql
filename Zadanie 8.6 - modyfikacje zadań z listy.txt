INSERT INTO POSTS (USER_ID, BODY)
VALUES (2, 'All users here are idiots!');

SET @last_id = LAST_INSERT_ID();

UPDATE POSTS
SET BODY = 'To delete'
WHERE ID = @last_id;

DELETE FROM POSTS
WHERE ID = @last_id;

COMMIT;